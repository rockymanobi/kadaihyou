<%= form_for(@issue_list) do |f| %>
  <% if @issue_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issue_list.errors.count, "error") %> prohibited this issue_list from being saved:</h2>

      <ul>
      <% @issue_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
